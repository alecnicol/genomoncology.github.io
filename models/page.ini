[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.sections]
name = Sections
type = flow
flow_blocks = hero, banner, infographic, grid, flexslider, tailored, capabilities, solutions, mcg, carousel

[fields.show_clients]
name = Show Clients
type = boolean
default = false